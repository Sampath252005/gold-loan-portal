export const validateApplication = (values) => { const errors = {}; const grossWeight = Number(values.grossWeightGrams); const netWeight = Number(values.netWeightGrams); if (values.customerName.trim().length < 2) errors.customerName = "Enter the customer's full name."; if (!/^\d{10}$/.test(values.mobileNumber)) errors.mobileNumber = "Enter a valid 10-digit mobile number."; if (!Number.isFinite(grossWeight) || grossWeight <= 0) errors.grossWeightGrams = "Enter a positive gross weight."; if (!Number.isFinite(netWeight) || netWeight <= 0) errors.netWeightGrams = "Enter a positive net weight."; if (grossWeight > 0 && netWeight > grossWeight) errors.netWeightGrams = "Net weight cannot exceed gross weight."; if (![18, 22, 24].includes(Number(values.purityKarat))) errors.purityKarat = "Select a valid gold purity."; if (!values.selectedPlanId) errors.selectedPlanId = "Choose a loan scheme to continue."; return errors; };
