import { CalendarDays, CheckCircle2, ChevronRight, Coins, Percent } from "lucide-react";

const LoanSchemeCard = ({ scheme, selectedPlanId, handlePlanSelect }) => {
  const isSelected = selectedPlanId === scheme.plan_code;
  const Icon = /emi|month/i.test(scheme.name) ? CalendarDays : Coins;
  return <button type="button" onClick={() => handlePlanSelect(scheme.plan_code)} className={`group relative w-full overflow-hidden rounded-2xl border p-4 text-left transition duration-200 focus:outline-none focus:ring-4 focus:ring-amber-100 ${isSelected ? "border-amber-400 bg-amber-50/60 shadow-[0_10px_25px_-18px_rgba(217,119,6,0.7)]" : "border-slate-200 bg-white hover:-translate-y-0.5 hover:border-amber-300 hover:shadow-md"}`}><div className="flex items-start justify-between gap-3"><span className={`grid size-9 place-items-center rounded-xl ${isSelected ? "bg-amber-500 text-white" : "bg-slate-100 text-slate-600 group-hover:bg-amber-50 group-hover:text-amber-700"}`}><Icon size={18} /></span>{isSelected ? <span className="inline-flex items-center gap-1 rounded-full bg-amber-100 px-2 py-1 text-[11px] font-bold text-amber-800"><CheckCircle2 size={13} /> Selected</span> : <ChevronRight size={18} className="text-slate-300 transition group-hover:translate-x-0.5 group-hover:text-amber-500" />}</div><h3 className="mt-3 text-sm font-bold text-slate-900">{scheme.name}</h3><p className="mt-0.5 text-[11px] font-semibold uppercase tracking-wide text-slate-400">{scheme.plan_code}</p><div className="mt-4 grid grid-cols-2 gap-3 border-t border-slate-100 pt-3"><div><span className="flex items-center gap-1 text-[11px] font-medium text-slate-500"><Percent size={12} /> Interest</span><p className="mt-0.5 text-base font-extrabold text-slate-900">{scheme.interest_rate}%</p></div><div><span className="text-[11px] font-medium text-slate-500">Maximum LTV</span><p className="mt-0.5 text-base font-extrabold text-slate-900">{scheme.max_ltv}%</p></div></div></button>;
};
export default LoanSchemeCard;
